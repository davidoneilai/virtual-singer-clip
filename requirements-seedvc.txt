# Minimal Seed-VC inference deps (no torch pins — reuse the container torch).
munch>=4.0.0
librosa>=0.10.2
einops>=0.8.0
scipy>=1.10.0
pyyaml
hydra-core>=1.3.2
python-dotenv
descript-audio-codec>=1.0.0
